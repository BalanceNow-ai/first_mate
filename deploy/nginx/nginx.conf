upstream api_backend {
    server api:8000;
}

server {
    listen 80;
    server_name _;

    # Health check endpoint (no redirect)
    location /health {
        proxy_pass http://api_backend/health;
    }

    # Redirect all HTTP to HTTPS (enable after certbot setup)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # Until SSL is configured, proxy directly
    location / {
        proxy_pass http://api_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Static files / Flutter web (if serving from this server)
    location /web/ {
        alias /var/www/flutter/;
        try_files $uri $uri/ /web/index.html;
    }

    client_max_body_size 20M;
}

# HTTPS server — uncomment after running certbot
# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location /health {
#         proxy_pass http://api_backend/health;
#     }
#
#     location / {
#         proxy_pass http://api_backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#
#     location /web/ {
#         alias /var/www/flutter/;
#         try_files $uri $uri/ /web/index.html;
#     }
#
#     client_max_body_size 20M;
# }
